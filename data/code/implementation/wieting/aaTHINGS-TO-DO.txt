THIS IS NOT COMPLETE

Things to complete to finish this implementation:
    - Move the pretrained models to a more appropriate directory.
    - Implement paraphrase-similarity in generation/ directory.
    - Method of evaluating all documents efficiently.
        - At the moment, need to pass in pairs of documents.
        - Goals (refine this):
            - Take in dataframe/list of N documents.
            - Return N x N similarity matrix.
